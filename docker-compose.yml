version: "3.9"

services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: arcgis-postgres
    environment:
      POSTGRES_DB: arcgis
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
